# Tag 6 — 08.06.2026

## Was habe ich heute gelernt?
- VS Code als grafische Git-Oberfläche entdeckt
- Git muss nicht immer in der PowerShell sein
- VS Code macht git add automatisch beim Commit
- git fetch — im Hintergrund prüfen ob GitHub Neuigkeiten hat
- Den Commit-Verlauf grafisch im GRAPH sehen
- U = Untracked, M = Modified im VS Code Source Control

## Welches Problem hatte ich?
git add . war in VS Code nicht nötig —
VS Code fragt automatisch ob es staged werden soll.

## Wie habe ich es gelöst?
"Always" geklickt — VS Code macht git add ab jetzt automatisch.

## Was nehme ich mit?
Git ist immer dasselbe — egal ob PowerShell oder VS Code.
VS Code macht es nur bequemer mit einer grafischen Oberfläche.
Der GRAPH zeigt die komplette Commit-Geschichte visuell —
genau wie git log --oneline, aber schöner.